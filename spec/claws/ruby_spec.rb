require "spec_helper"

RSpec.describe Claws::Ruby do
  it "has a version number" do
    expect(Claws::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
